# 먼지몽 알림

귀여운 미세먼지 알림 앱입니다.

## 파일 구조

- `index.html`: 화면 구조
- `styles.css`: 디자인과 애니메이션
- `app.js`: 상태 변경, 알림, 먼지 클릭 동작

## 같이 수정하는 방법

1. `main`에서 새 브랜치를 만듭니다.
2. 새 브랜치에서 파일을 수정합니다.
3. 수정한 내용을 커밋합니다.
4. Pull request를 만들어 `main`에 합칩니다.

작은 실험은 브랜치에서 마음껏 해도 됩니다. 마음에 들지 않으면 합치지 않으면 됩니다.
